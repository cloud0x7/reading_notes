《Python参考手册》
=============================

### 作者
  【美】David M.Beazley 【译】谢俊 / 杨越 / 高伟   
### 简介
> 本书是权威的Python语言参考指南，内容涉及核心Python语言和Python库的最重要部分。本书内容简洁扼要、可读性强，书中还包括了一些没有在Python官方文档或其他资料中出现过的一些高级的主题。
这一版在内容上进行了全面更新，介绍了Python 2.6和Python 3新引入的编程语言特性和库模块，同时还分析了Python程序员面临的如下难题：是应继续使用现有的Python代码，还是应制定计划将其移植到Python 3？

### 豆瓣链接
[豆瓣链接](http://book.douban.com/subject/5401851/)


### 目录
* 第一部分 Python语言
  - 第01章 Python简介
  - 第02章 词汇和语法约定
  - 第03章 类型与对象
  - 第04章 运算符与表达式
  - 第05章 程序结构与控制流
  - 第06章 函数与函数编程
  - 第07章 类与面向对象编程
  - 第08章 模块、包与分发
  - 第09章 输入与输出
  - 第10章 执行环境
  - 第11章 测试、调试、探查与调优   
* 第二部分 Python库 
  - 第12章 内置函数和异常
  - 第13章 Python运行时服务
  - 第14章 数学运算
  - 第15章 数据结构、算法与代码简化
  - 第16章 字符串和文本处理
  - 第17章 Python数据库访问
  - 第18章 文件和目录处理
  - 第19章 操作系统服务
  - 第20章 线程与并发性
  - 第21章 网络编程和套接字
  - 第22章 Internet应用程序编程
  - 第23章 Web编程
  - 第24章 Internet数据处理和编码
  - 第25章 其他库模块
* 扩展与嵌入
  - 第26章 扩展与嵌入Python

### 内容
* 第一部分 Python语言
  - 第01章 Python简介
  - 第02章 词汇和语法约定
  - 第03章 类型与对象
    - 列表
      - list(s)
      - append(x)
      - extend(l)
      - count(x)
      - index(x [,start])
      - insert(i, x)
      - pop([i])
      - remove(x)
      - reverse()
      - sort([key [, reverse]])
    - 字符串
      - s.captitalize(), s.title()
      - s.center(width, [,pad])
      - s.count(sub [, start [, end]])
      - s.decode(), s.encode()
      - s.endswith(), s.startswith(prefix [,start [,end]])
      - s.expandtabs()
      - s.find()
      - s.format()
        - '{0:.{1}f'.format(1 / 3.0, 5)
      - s.index()
      - s.isalnum(), s.isalpha(), s.isdigit()
      - s.islower(), s.isspace(), s.isupper()
      - s.join(), 
      - s.ljust(width [, fill]), s.rjust(width, [,fill])
      - s.lower(), s.upper(), s.swapcase()
      - s.lstrip([chrs]), s.rstrip([chrs])
      - s.partition(sep), s.rpartition(sep)
      - s.replace(old, new [,maxreplace])
      - s.rfind(sub [,start [,end]])
      - s.split(sep [,maxsplit]), s.rsplit(sep [,maxsplit])
      - s.zfill(width)
    - 字典
      - len(m)
      - m[k], m[k] = x, del m[k]
      - k in m
      - m.clear()
      - m.copy()
      - m.fromkeys(s [,value])
      - m.get(k [,v])
      - m.has_key(k)
      - m.items()
      - m.keys()
      - m.pop(k [,default]), m.popitem()
      - m.setdefault(k [,v])
      - m.update(b)
      - m.values()
    - 集合
      - len(s)
      - s.copy()
      - s.difference(t)   # 求差集
      - s.intersection(t) # 求交集
      - s.isdisjoint(t)    
      - s.issubset(t), s.issuperset(t)
      - s.symmetric_difference(t)
      - s.unit(t)         # 求并集
      - s.add(item)
      - s.clear()
      - s.pop(), s.remove(item), s.update(t)
      - ...
    - 函数
      - 属性
        - f.`__doc__`
        - f.`__name__`
        - f.`__dict__`
        - f.`__code__`
        - f.`__defaults__`
        - f.`__globals__`
        - f.`__closure__`
      - 方法
        - def func():   # 实例方法
        - @classmethod  #类方法
        - @staticmethod # 静态方法
  - 第04章 运算符与表达式
    - 序列操作
      - s * n
      - all(s), any(s)
      - min(s), max(s), sum(s)
    - 字典操作
      - x = d[k]
      - d[k] = x
      - del d[k]
    - 集合操作
      - s | t  #并集
      - s & t  #交集
      - s - t  #差集
      - s ^ t  #对称差集
  - 第05章 程序结构与控制流
    - for key, value in enumerate(s):
    - class NetWorkError(Exception):pass   
  - 第06章 函数与函数编程
    - a = 10; def foo(x = a):
    - def foo(x, items=None): # 可变对象最好设为None
    - def foo(x, *args):      # 任意数量参数
    - def foo(*args, `**`kwargs): # 额外的关键字参数
    - foo(x=3, y=22)          # **关键字参数调用**，参数顺序无关紧要
    - def foo(): global a     # 全局变量
    - 装饰器：包装另一个函数或类；可同时使用多个装饰器；装修器可以有参数
      - def trace(param):
      - @trace
      - def func():
    - 生成器与yield
    - 协程与yield表达式：
      - def receiver(): while True:n = (yield);print('Got %s' % n)
      - r = receiver(); r.next(); r.send(1)
    - **列表推导**
      - [expression for item in iterable if condition]
    - 生成器表达式
      - (expression for item in iterable if condition)
      - 生成器，更节省内存
    - lambda args: expression
    - 递归
      - sys.getrecursionlimit() # 递归最大深度
      - sys.setrecursionlimit() # 修改递归最大深度，默认1000，超出深度引发RuntimeError
    - eval(), exec(), compile()
  - 第07章 类与面向对象编程
    - class A(B, C, D): # 支持多重继承
    - class A(B): def `__init__`(self): B.`__init__`(self)
    - Class.`__`mro`__`  # 继承顺序
    - 多态：**在不考虑实例类型的情况下使用实例**
    - @staticmethod  # 静态方法
    - @classmethod def fun(cls):pass  # 类方法
    - 属性
      - @property
      - @{prop}.setter
      - @{prop}.deleter
      - `__`slots`__` = ('name', 'balance') # 阻止实例添加新属性，内存及性能优化（实例不再使用字典存储数据，而使用基于数组的数据结构） 
    - 私有属性
      - self.`__`{prop}
    - 类型和成员测试
      - isinstance(a, A)
      - issubclass(Foo, IFoo)
    - 抽象基类
      - from abc import ABCMeta, abstractmethod, abstractproperty
      - ``__``metaclass``__`` = ABCMeta
      - @abstractmethod
      - @abstractproperty
    - 类装饰器
      - `__`clsid`__`
  - 第08章 模块、包与分发
    - 模块
      - 导入
        - import spam as sp  # 导入
        - from spam import foo	 # 导入选定符号
        - from spam import *   # 导入除下划线开头以外的所有
      - 主程序执行
        - if `__`name`__` == '`__`main`__`'
      - 模块搜索路径
        - sys.path.append('modules.zip')
        - 支持zip，egg，py,pyw, pyc, pyo文件
    - 包
      - 第一次导入包，就会执行文件`__`init`__`.py
      - `__`all`__` = [] 定义其中子模块
  - 第09章 输入与输出
    - import sys; sys.argv
    - import os; os.environ[{name}]
    - 文件
      - f.read(), f.readline(), f.readlines()
      - f.write(), f.writelines()
      - f.close()
      - f.tell()
      - f.seek()
      - f.flush()
      - f.truncate()
      - f.fileno()
      - f.next()
    - 标准输入输出
      - sys.stdout.write('enter your name')
      - name = sys.stdint.readline()
      - raw_input() # py2
      - input()     # py3
      - sys.stderr
      - 'Dear `%`(name)'  # 变量插入
      - import string; string.Template('''$name''')
    - 对象序列化
      - import pickle
        - pickle.dump(obj, open(filename, 'wb'))
        - obj = pickle.load(open(filename, 'rb'))
      - import shelve
        - db = shelve.open(filename)
        - db['key'] = value
        - db.close()
  - 第10章 执行环境
  - 第11章 测试、调试、探查与调优   
* 第二部分 Python库 
  - 第12章 内置函数和异常
  - 第13章 Python运行时服务
    - import atexit; atexit.register(func); 解释器退出时反序执行
    - import copy; copy.copy(x); copy.deepcoyp(x, [,visit])
    - gc
    - inspect收集Python活动对象的信息，如属性文档源码等
    - marshal 类似于pickle和shelve，序列化Python对象
    - sys.exit()
    - traceback
    - types
    - warnings.warn()
    - weakref
  - 第14章 数学运算
    - decimal
    - math
    - random
  - 第15章 数据结构、算法与代码简化
    - ABCMeta 用于定义新的抽象基类
    - array
    - bisect
    - collections
    - deque defaultdict
    - contextlib
    - functools
    - heapq
    - itertools
  - 第16章 字符串和文本处理   
    - codecs 处理各种字符编码
    - re 正则
      - r.compile()
      - r.flags
      - r.groupindex
      - r.pattern
      - r.findall
      - r.finditer
      - r.match
      - r.search
      - r.split
      - r.sub
      - r.subn 
    - string
    - Formatter
    - Template
    - 实用工具函数
      - capwords(s)
      - maketrans(from, to)
    - struct
      - pack()
      - pack_into()
      - unpack()
      - unpack_from()
      - calcsize()
  - 第17章 Python数据库访问
    - c = connect(dsn="hostname:...", user="...", password="...")
      - c.close()
      - c.commit()
      - c.rollback()
      - c.cursor()
    - Cursor
      - cur.execute(query, [,parameters])
  - 第18章 文件和目录处理
    - bz2
      - BZ2File(filename [, mode [, buffering [, 	compresslevel]]])
      - BZ2Compressor([compresslevel])
      - c.compress(data)
      - c.flush()
      - BZ2Decompressor()
      - d.decompress(data)
      - compress(data [, compresslevel])
      - decompress(data)
    - filecmp, 用于比较文件和目录
      - cmp(file1, file2 [,shallow])
      - cmpfiles(dir1, dir2, common [, shallow])
      - dircmp(dir1, dir2 [, ignore [, hide]])
      - d.report()
      - d.report_partial_closure()
      - d.report_full_closure()
      - d.left_list
      - d.right_list
      - d.common
      - d.left_only
      - d.right_only
      - d.common_dirs
      - d.common_dirs
      - d.common_files
      - d.common_funny
      - d.same_files
      - d.diff_files
      - d.funny_files
      - d.subdirs
    - fnmatch
      - fnmatch(filename, pattern)
      - fnmatchcase(filename, pattern)
      - filter(names, pattern)
    - glob
      - htmlfile = glob('*.html')
    - gzip
      - GzipFile(filename, [, mode [, compressleve [,fileobj]]])
      - open(filename [, mode [, compresslevel]]])
    - shutil
      - copy(src, dst)
      - copy2(src, dst) // 复制了最后访问时间和修改时间
      - copyfile(src, dst)
      - copyfileobj(f1, f2 [, length])
      - copymode(src, dst)
      - copystat(src, dst)
      - copytree(src, dst, symlinks [, ignore])
      - move(src, dst)
      - rmtree(path [, ignore_errors [, onerror]])
    - tarfile 用于处理tar文件
      - is_tarfile(name)
      - open([name [, mode [, fileobj [, bufsize]]]])
      - t.add(name [, arcname [, recursive]])
      - t.addfile(tarinfo [, fileobj])
      - t.close()
      - t.debug
      - t.dereference
      - t.errorlevel
      - t.extract(member [, path])
      - t.extractfile(member)
      - t.getmember(name)
      - t.gemembers()
      - t.getnames()
      - t.gettarinfo(...)
    - tempfile 用于生成临时性文件名和文件
    - zipfile 
    - zlib 提供对zlib库的访问支持数据压缩
  - 第19章 操作系统服务
    - Commands模块
      - commands.getoutput('ls -l')
    - ConfigParser模块用于读取ini格式的配置文件
      - from configparser import ConfigParser
      - cfg = ConfigParser({'basedir': '/Users/abc/app')
      - cfg.read('app.ini')
      - cfg.get('output')
    - datetime
      - date
        - date(year, month, day)
        - date.today()
        - date.fromtimestamp(timestamp)
        - date.fromordinal(ordinal)
        - date.min, date,max, date,resolution
        - d.ctime()
        - d.isocalendar(), d,isoformat(), d.isoweekday()
        - d.replace()
        - d.strttime(format)
        - d.timetuple()
        - d.toordinal()
        - d.weekday()
      - time
      - datetime
      - timedelta 对象表示两个时间差
    - tzinfo对象 时区，仅仅是一个基类
    - errno
    - fcntl模块对UNIX文件描述符执行文件和I/O控制
    - io模块
    - logging模块
    - Logger对象
    - mmap模块为内存映射的文件对象提供支持
    - optparse
    - os模块
  - 第20章 线程与并发性
    - multiprocessing
    - threading
    - Timer对象
    - Lock对象
  - 第21章 网络编程和套接字
  - 第22章 Internet应用程序编程
  - 第23章 Web编程
  - 第24章 Internet数据处理和编码
  - 第25章 其他库模块
* 扩展与嵌入
  - 第26章 扩展与嵌入Python